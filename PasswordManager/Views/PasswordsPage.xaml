<Page x:Class="PasswordManager.Views.PasswordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PasswordManager.Views"
      xmlns:vm ="clr-namespace:PasswordManager.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600" FontFamily="{StaticResource MainFont}"
      Title="PasswordsPage">

    <Page.DataContext>
        <vm:PasswordsViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <ResourceDictionary Source="ResourceDictionary.xaml"/>
    </Page.Resources>

    <Grid Background="{StaticResource BackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" FontSize="28" Foreground="{StaticResource ControlsColor}" Content="Your own password manager" 
           VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <ListBox ItemsSource="{Binding FilteredItems, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" SelectedItem="{Binding SelectedAccount}" Grid.Row="1" Grid.ColumnSpan="2" Grid.RowSpan="3" Margin="5 45 5 5" FontSize="18">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <Border HorizontalAlignment="Left" Width="40" Height="30" CornerRadius="10" Background="{StaticResource BackColor}" Margin="5 0">
                            <Image Source="{Binding IconPath}" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>
                        <Label Content="{Binding Url}" FontSize="22" Foreground="{StaticResource ForeColor}"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBox x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MainTextBox}" Width="35" HorizontalAlignment="Left" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" Padding="30 5 30 5" Height="35" VerticalAlignment="Top" FontSize="18"/>

        <Button x:Name="AddButton" Style="{StaticResource BaseButtonStyle}" Content="Add" Grid.Row="4" Grid.Column="0" Margin="10" FontSize="20" Click="AddButton_Click"/>
        <Button Command="{Binding RemoveAccountCommand}" Style="{StaticResource BaseButtonStyle}" Content="Remove" Grid.Row="4" Grid.Column="1" Margin="10" FontSize="20"/>
        <Button x:Name="ChangeButton" Command="{Binding ChangeAccountCommand}" Style="{StaticResource BaseButtonStyle}" Content="Change" Grid.Row="4" Grid.Column="2" Margin="10" FontSize="20" Click="ChangeButton_Click"/>
        <Button Command="{Binding ImportDataCommand}" Style="{StaticResource BaseButtonStyle}" Content="Import" Grid.Row="4" Grid.Column="3" Margin="10" Width="100" FontSize="20"
                HorizontalAlignment="Left"/>
        <Button Command="{Binding ExportDataCommand}" Style="{StaticResource BaseButtonStyle}" Content="Export" Grid.Row="4" Grid.Column="3" Margin="10" Width="100" FontSize="20"
                HorizontalAlignment="Right"/>

        <Button Style="{StaticResource BaseButtonStyle}" Content="&lt;&lt;" Grid.Row="0" Grid.Column="0" Margin="10" FontSize="20" FontFamily="{StaticResource AdditiveFont}"
                HorizontalAlignment="Left" Width="50" Click="Button_Click"/>

        <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Grid.RowSpan="3" Margin="5">
            <StackPanel Margin="5">
                <Label Content="Username: " FontSize="24" Foreground="{StaticResource ForeColor}" Padding="5" Margin="10 0"/>
                <DockPanel>
                    <TextBox Style="{StaticResource MainTextBox}" Text="{Binding SelectedAccount.Username}" FontSize="22" MinWidth="280" MaxWidth="280" Margin="10 10 0 10" Padding="5" VerticalAlignment="Center" IsReadOnly="True"/>
                    <Button Command="{Binding CopyDataCommand}" CommandParameter="Username" Style="{StaticResource AnimatedCopyButtonStyle}" Content="{StaticResource CopyImage}" BorderThickness="0" HorizontalAlignment="Right" Click="CopyButton_Click"/>
                </DockPanel>
            </StackPanel>

            <StackPanel Margin="5">
                <Label Content="Password: " FontSize="24" Foreground="{StaticResource ForeColor}" Padding="5" Margin="10 0"/>
                <DockPanel>
                    <Grid>
                        <TextBox Style="{StaticResource MainTextBox}"  Text="{Binding SelectedPassword}" FontSize="22" MinWidth="280" MaxWidth="280" Margin="10 10 0 10" Padding="5 5 45 5" VerticalAlignment="Center" IsReadOnly="True"/>
                        <!--<Image Source="C:\Users\Evan\Desktop\eye.png" Width="30" HorizontalAlignment="Right" Margin="10"/>-->
                        <Button Style="{StaticResource AnimatedButtonStyle}" Command="{Binding RevealPasswordCommand}" Content="{Binding RevealImg}" BorderThickness="0" Width="30" Height="30" HorizontalAlignment="Right" Margin="10"/>
                    </Grid>

                    <!--<Image Source="C:\Users\Evan\Desktop\copy.png" Width="30" HorizontalAlignment="Right" Margin="5"/>-->
                    <Button Command="{Binding CopyDataCommand}" CommandParameter="Password" Style="{StaticResource AnimatedCopyButtonStyle}" Content="{StaticResource CopyImage}" BorderThickness="0" HorizontalAlignment="Right" Click="CopyButton_Click"/>
                </DockPanel>
            </StackPanel>

            <StackPanel Margin="5">
                <Label Content="Website: " FontSize="24" Foreground="{StaticResource ForeColor}" Padding="5" Margin="10 0"/>
                <DockPanel>
                    <TextBox Style="{StaticResource MainTextBox}"  Text="{Binding SelectedAccount.Url}" FontSize="22" Foreground="{StaticResource ForeColor}" Margin="10 10 0 10" Padding="5" MinWidth="280" MaxWidth="280" VerticalAlignment="Center" IsReadOnly="True"/>
                    <!--<Image Source="C:\Users\Evan\Desktop\copy.png" Width="30" HorizontalAlignment="Right" Margin="5"/>-->
                    <Button Command="{Binding CopyDataCommand}" CommandParameter="Website" Style="{StaticResource AnimatedCopyButtonStyle}" Content="{StaticResource CopyImage}" BorderThickness="0" HorizontalAlignment="Right" Click="CopyButton_Click"/>
                </DockPanel>
            </StackPanel>
        </StackPanel>
        <Button Style="{StaticResource AnimatedButtonStyle}" Grid.Column="0" Content="{StaticResource SearchImage}" Width="30" Height="30" HorizontalAlignment="Left" Margin="8 8" Grid.Row="1" VerticalAlignment="Top" Click="SearchButton_Click"/>
        <Button x:Name="ResetButton" Command="{Binding ResetSearchCommand}" Style="{StaticResource AnimatedButtonStyle}" Grid.Column="1" Content="X" Width="30" Height="30" HorizontalAlignment="Right" Margin="7 8" Grid.Row="1" VerticalAlignment="Top" Visibility="Hidden"/>

        <Border x:Name="NotificationBorder" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" CornerRadius="10"
                VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="30" Margin="10"
                Background="{StaticResource ForeColor}" Opacity="0">
            <Label Content="{Binding InfoText}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
        </Border>
    </Grid>
</Page>
