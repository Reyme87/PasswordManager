<Page x:Class="PasswordManager.Views.NotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PasswordManager.Views"
      xmlns:vm="clr-namespace:PasswordManager.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600" FontFamily="{StaticResource MainFont}"
      Title="NotesPage">

    <Page.DataContext>
        <vm:NotesViewModel/>
    </Page.DataContext>
    
    <Page.Resources>
        <ResourceDictionary Source="ResourceDictionary.xaml"/>
    </Page.Resources>

    <Grid Background="{StaticResource BackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" FontSize="28" Foreground="{StaticResource ControlsColor}" Content="Your own notes manager"
               VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <ListBox ItemsSource="{Binding Notes, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" SelectedItem="{Binding SelectedNote}" Grid.Row="1" Grid.ColumnSpan="2" Grid.RowSpan="3" Margin="5" FontSize="18">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" FontSize="22" Foreground="{StaticResource ForeColor}" MaxWidth="190" MinWidth="190" TextTrimming="CharacterEllipsis"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button x:Name="AddButton" Style="{StaticResource BaseButtonStyle}" Content="Add" Grid.Row="4" Grid.Column="0" Margin="10" FontSize="20" Click="AddButton_Click"/>
        <Button Command="{Binding RemoveNoteCommand}" Style="{StaticResource BaseButtonStyle}" Content="Remove" Grid.Row="4" Grid.Column="1" Margin="10" FontSize="20"/>
        <Button Command="{Binding ChangeNoteCommand}" x:Name="ChangeButton" Style="{StaticResource BaseButtonStyle}" Content="Change" Grid.Row="4" Grid.Column="2" Margin="10" FontSize="20" Click="ChangeButton_Click"/>
        <Button Command="{Binding ImportDataCommand}" Style="{StaticResource BaseButtonStyle}" Content="Import" Grid.Row="4" Grid.Column="3" Margin="10" Width="100" FontSize="20"
                HorizontalAlignment="Left"/>
        <Button Command="{Binding ExportDataCommand}" Style="{StaticResource BaseButtonStyle}" Content="Export" Grid.Row="4" Grid.Column="3" Margin="10" Width="100" FontSize="20"
                HorizontalAlignment="Right"/>

        <Button Style="{StaticResource BaseButtonStyle}" Content="&lt;&lt;" Grid.Row="0" Grid.Column="0" Margin="10" FontSize="20" FontFamily="{StaticResource AdditiveFont}"
                HorizontalAlignment="Left" Width="50" Click="Button_Click"/>

        <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Grid.RowSpan="3" Margin="5">
            <StackPanel Margin="5">
                <Label Content="Name: " FontSize="24" Foreground="{StaticResource ForeColor}" Padding="5" Margin="10 0"/>
                <TextBox Style="{StaticResource MainTextBox}" Text="{Binding SelectedNote.Name}" FontSize="22" Margin="10" Padding="5" VerticalAlignment="Center" IsReadOnly="True"/>
            </StackPanel>

            <StackPanel Margin="5">
                <TextBox Style="{StaticResource MainTextBox}" Text="{Binding SelectedNote.Text}" FontSize="22" Margin="10" Padding="5" TextWrapping="Wrap" AcceptsReturn="True" Height="200" MaxHeight="200" VerticalContentAlignment="Top" VerticalAlignment="Stretch" IsReadOnly="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
