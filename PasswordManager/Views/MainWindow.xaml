<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="600" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">

    <Frame Name="MainFrame" NavigationUIVisibility="Hidden"/>
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="0.4*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Content="Your password manager" 
               VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="28"/>

        <ListBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="2"
                 Background="Beige" d:ItemsSource="{d:SampleData ItemCount=5}" FontSize="20"/>

        <Button Grid.Row="3" Grid.Column="1" Margin="10" Content="Add" FontSize="24"/>
        <Button Grid.Row="3" Grid.Column="2" Margin="10" Content="Remove" FontSize="24"/>
    </Grid>-->
</Window>
